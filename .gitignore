# Windows- und OneDrive-Spuren
Thumbs.db
desktop.ini
*.tmp

# Office-Sperrdateien, falls hier doch mal eine Vorlage liegt
~$*

# macOS, falls die Dateien mal auf einem Mac landen
.DS_Store

# Persoenliche Claude-Code-Einstellungen dieses Rechners.
# Enthaelt den Stop-Hook, der nach einer Sitzung automatisch pusht, samt
# absolutem Pfad. Gehoert nicht ins oeffentliche Repo: wer die Sammlung
# klont, soll nicht ungefragt in dieses Repository pushen.
.claude/settings.local.json
